VITE_DUMMY_APIS=true
VITE_API_URL=http://localhost:3001/api

# Tells Nitro (TanStack Start's server engine) to output a Vercel-compatible
# serverless build. Without this, Vercel gets a plain static bundle with no
# server handler and returns 404 on every route.
NITRO_PRESET=vercel
